export type CategoryConfig = {
  TradeAgreement: {
    label: string;
    icon: React.ReactNode;
  };
  Tariff: {
    label: string;
    icon: React.ReactNode;
  };
  TradePolicy: {
    label: string;
    icon: React.ReactNode;
  };
  TradeEvent: {
    label: string;
    icon: React.ReactNode;
  };
};
